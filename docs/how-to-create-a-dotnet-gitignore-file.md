# How To: Create a .NET `.gitignore` File

The `dotnet` tool has a `new` template for adding a `.gitignore` file:

```
dotnet new gitignore
```

I usually add this to either the repo root or my root `/src` directory.